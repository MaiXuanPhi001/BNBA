<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Binance" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/black2</item>
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <!-- Dark mode   -->
    <!-- Screen background if dark mode on   -->
    <style name="SCREEN">
        <item name="android:background">#232323</item>
    </style>

    <!--  Text background if dark mode on  -->
    <style name="TEXT">
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="BUTTON">
        <item name="android:background">#B9BABA</item>
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Dark mode   -->
    <style name="back_ground">
        <item name="background">@color/black2</item>
    </style>
    <style name="text_black">
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="background_gray3">
        <item name="android:background">@color/gray3_dark</item>
    </style>
    <style name="button_login_home">
        <item name="backgroundTint">@color/black2</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="input_login">
        <item name="android:background">@color/gray4_dark</item>
    </style>
</resources>